# AI协作工作流协议 v3

适用工具：Claude网页端 / Claude Code / CodeBuddy / Codex / zCode
存放位置：项目根目录 `docs/agent-workflow-protocol.md`
配套文件：项目根目录放 `CLAUDE.md`（Claude Code自动读取）/ `AGENTS.md`（其他支持此约定的工具自动读取），内容是指向本文件的一句话指引。

> v3 相较 Lite v0.1 的变化：以Lite的自适应结构为底座（v2停止维护，两条文件树合并为一条）；新增"外部材料信源分级与隔离"机制，处理"带着其他模型产出的半成品/对话记录来continue"的场景；新增"产品功能地图"，合并原"需求池"，覆盖"讨论中识别到新功能模块"的场景，不再要求功能规划必须一次性做完才能开工。
> 本次修订（补丁，未改版本号）：修正隔离区"禁止读取"规则——此前误写成"用户在当前消息明确要求即可读"，与设计初衷相反，改为本对话内无论用户如何要求都不读；补充功能子目录不由AI默认自动创建，改为展示模块地图供用户定位。

---

## 一、九条核心原则

1. **草案优先，而非提问优先**——拿到需求先产出完整草案，用户改草案，不填问卷。
2. **大切小 + 模块地图**——需求足够宽泛时先切一个端到端能跑通的最小价值切片；讨论中识别到新的独立功能模块时，提出记入《产品功能地图》（一句话描述+建议状态），经用户确认才写入；不要求模块规划做完才能开发，未深挖的模块可以标"占位-标准实现"先用行业惯例做出来，优先让MVP尽快可用。开始处理某个具体功能前，若地图已存在，先展示已确认模块列表帮用户定位；**功能子目录不由AI默认自动创建**，由用户手动建或明确指示后AI才建。
3. **外部材料信源分级**——用户导入其他模型产出的材料（半成品需求文档、对话记录、提示词历史等）时，按"用户原话/用户明确认可/AI单方面提出未被确认"分三级采信，只有一二级进入当前工作流上下文，三级默认隔离存档、不带入讨论。
4. **问题看必要性，不看数量**——该问的问，不该问的不问，不设数量上限；每个问题用"选择题+推荐答案"格式，答不上来就用推荐项转成❓假设继续往下走。
5. **翻译层**——需要用户拍板的节点只讲体验/成本/时间/风险，不甩技术选型细节；附推荐+理由。
6. **轻量启动，按需扩展**——默认走最短路径：需求定稿 → 设计定稿 → 开发。探索机制、项目级共享文件只在真正触发条件出现时才启用/创建。
7. **检查点确认，门禁不锁死**——阶段推进要用户确认；发现上游有误随时可回溯，走版本升级，不是打回重来。
8. **需求资产与认知资产分离**——探索中学到的东西默认写进探索日志，不改需求正文；只有证伪需求本身的假设，才把需求升版本。
9. **文件即记忆**——一切定稿必须落盘，不依赖任何工具的对话上下文。

---

## 二、反Plan Mode：三个具体机制

### ① 草案优先
拿到需求后第一动作永远是产出完整草案，不确定的地方用行内标记：
```
积分有效期：默认永久有效 ❓假设——如需过期规则请直接改这一行
```

### ② 问题处理规则（不设数量上限）
- **何时问**：同时满足"会实质改变需求方向或边界"+"只有用户能决定、AI无法用合理假设代替"，才问。
- **怎么问**：选择题形式，附AI推荐项和理由，不用开放式提问。
- **答不上来怎么办**：用户说"不知道/你定/先这样"，AI直接采用推荐项，标❓假设，继续产出草案，不再追问同一个点。
- **防止滑回Plan Mode的唯一硬规则**：连续两轮只提问、没有产出任何草案或文档内容，视为违规，下一轮必须无条件先给出草案。

### ③ 大切小（切片协议）
用户提出一个宽泛功能时，AI第一动作是识别出一个**端到端可跑通的最小价值切片**，只针对这个切片走完整流程，其余部分整理进《产品功能地图》（见第五节）。

---

## 三、外部材料信源分级与隔离

### 触发条件
用户消息明确标注"外源模型信息"（或等价明确说明），才启动本流程。**没有明确标注，一律按用户本人第一人称陈述处理，不触发分级**。

### 处理步骤
1. 通读导入的材料，逐条判断信源级别：
   - **一级（直接采用）**：材料里用户自己主动说出的话——原始描述、举的例子、提出的限制条件
   - **二级（保留但复述确认）**：AI在材料中提出、用户明确认可的内容——用AI自己的话重新表述，不沿用原表述，之后仍按正常流程向用户确认
   - **三级（默认丢弃）**：AI单方面提出、用户没有明确回应、或对话直接滑过去的内容
2. 一二级内容 → 按Skill A正常"意图还原"流程整理成草稿
3. 三级内容 → **不进入当前工作流上下文**，整理后写入一份新文件：`docs/外源信息隔离区/<日期>-导入<序号>-丢弃项.md`（序号=该目录下已有文件数+1；确定序号只需列出文件名计数，不构成"读取内容"，不违反下方规则），格式见 `templates/外源信息隔离区-丢弃项模板.md`。写入后在 `docs/外源信息隔离区/_索引.md` 追加一行（文件名+日期+一句话来源说明），不摘录丢弃内容本身。
4. **写入后，本对话内永久禁止读取**：文件写入完成后，AI在本次对话的所有后续轮次中，**无论用户如何要求，都不读取该文件内容**——即使用户在本对话中明确、逐字要求查看，也应告知这是协议规定的隔离区，本对话内不会读取，请自行查阅该文件，或开启一个新的对话请求读取。这是本协议唯一一条"用户明确要求也不执行"的规则，目的是让隔离真正起到隔离作用，避免讨论中途被反复说服而重新引入已判定为噪音的内容。
5. **多次导入**：整个协作过程中可能多次导入外部材料，每次都是独立事件，独立生成一个新文件，不追加进已有文件。

### 诚实说明
这是写进本协议的行为约束，AI会尽力严格遵守，但属于指令层面而非操作系统级的文件权限——不能100%技术保证。如需更硬的隔离，请自行在文件系统层面把该目录设为只读，或移出项目目录。

---

## 四、文件骨架与"按需创建"触发条件

**默认最小路径**（大多数功能都走这条）：
```
docs/
  agent-workflow-protocol.md
  PROGRESS.md
  <功能名>/
    01-requirements-v0.1.md   意图还原 + 需求正文 + 验收标准，合一文件
    03-design-v0.1.md         功能设计定稿
```
> 功能子目录（`docs/<功能名>/`）不由AI默认自动创建——由用户手动建，或用户明确指示后由AI创建。若已有《产品功能地图》，AI会在开始处理某个功能前先展示已确认模块列表帮助定位（见Skill A）。

**按需追加**：

| 文件/目录 | 触发条件 |
|---|---|
| `<功能名>/02-exploration-log.md` | Skill B判断"存在真正的技术不确定性"，需要走分支验证 |
| `docs/00-环境约束.md` | 某个设计决策被服务器/网络/端限制卡住，且判断以后其他功能也会遇到 |
| `docs/产品功能地图.md` | 首次"大切小"切出排队部分，或讨论中首次识别到独立功能模块 |
| `docs/decision-log.md` | 某次清算发现的认知被判断为"以后做别的功能也用得上" |
| `docs/外源信息隔离区/<日期>-导入N-丢弃项.md` | 每次导入被标注"外源模型信息"的材料且产生三级内容时 |

---

## 五、阶段自检固定格式（唯一定义，各Skill文件引用本节）

```
📍 阶段自检 | 当前阶段：<阶段名>
✅ <已完成项>
⬜ <未完成项>
状态：<全部完成 / 还有N项未完成>
👉 说"下一步"进入<下一阶段>；或直接补充/修正，我继续处理这一项。
```
用户说"跳过/先这样/进下一步" → 不追问、不重复提醒，⬜项写入下一阶段"遗留项"，正常推进。

---

## 六、清算模板（唯一定义，仅Skill B判断需要探索时使用）

```markdown
### 分支：explore/xxx （日期）
- 假设：
- 验证方法：
- 结果：成功 / 失败 / 部分成立
- 证据：
- 习得信息（是否有跨需求价值？有则同步写入 docs/decision-log.md）：
- 处置：分支删除 / 归档，产出物：<代码路径或"无">
```

---

## 七、三个Skill

详见 `skills/` 目录。Skill A新增"外部材料导入"入口、"产品功能地图"写入动作、开始新功能前的模块定位提示，Skill B/C基本不变。

---

## 八、回溯协议

```
任何阶段发现上游文档有误 →
1. 说明：错在哪、怎么发现的、影响范围
2. 升版本号（如 v0.1→v0.2），新文件头部写清"相较上一版本的变更"，v0.1永久保留不覆盖
3. 标注受影响的下游文档
4. 用户确认后同步修正
```
"回滚"回到的是**当前有效的需求版本**，不是固定回到最初的v0.1。

---

## 九、工具分配建议

| 工具 | 定位 | 使用时机 |
|---|---|---|
| Claude网页端 | Skill A 可选战场 | 纯聊需求，产出后需手动存成本地文件 |
| Claude Code | Skill A/B/C 均可，能直接读写本地文件 | 想省去手动搬运这一步，建议需求澄清也在这里跑 |
| Codex / CodeBuddy / zCode | 按需的第二意见 | 高风险假设交叉验证 / 开发完成后独立代码审查 |
